<!--
    Title: header.ejs
    Author: Manel Phiseme
    Date: 6/19/2022
    Description: Page header info.
-->
<header id="hero-header">
    <h1>Pets-R-Us</h1>
                                     
    <nav id="menu">
        <a class="menlist" href="/">Home</a> 
        <div class="subMenu">
            <span class="menlist"><a href="">Services</a></span>
            <div class="sublist">
                <a href="boarding">Boarding</a>                        
                <a href="grooming">Grooming</a>
                <a href="training">Training</a>
            </div>                            
        </div>
        <div style="text-align: right; float: right">          
        </div>
        <% if(currentUser) { %>
        <a class="menlist" href="/schedule">Booking</a> 
        <% } %>  
        <div class="subMenu"> 
            <% if(!currentUser) { %>
                <span class="menlist"><a href="" >Account</a></span> 
            <% } %> 
            <% if(currentUser) { %> 
                <span class="menlist"><a href="" >My Account</a></span>
            <% } %>  
            <div class="sublist">
                <% if(currentUser) { %>
                    <a href="#">Hi <%= currentUser %></a> 
                    <a href="/profile">Profile</a>
                    <a href="/schedule">Schedule Appt.</a>
                    <a href="/logout">Logout</a>
                <% } %>
                <% if(!currentUser) { %>
                    <a href="register" >Register</a>
                    <a href="login">Login</a> 
                <% } %>                                         
            </div>                        
        </div>                    
    </nav>  
    <div id="content">                        
        <h2 id="descript">Trusted for years by Everyone</h2>
    </div>                
</header>